/* components/tabs/tabs.wxss */

.title {
  display: flex;
}

.title_item {
  display: flex;
  justify-content: center;
  align-items: center;

  flex: 1;
  
  padding: 15rpx 0;
}

.active {
  color: rgb(235, 68, 80);
  border-bottom: 5rpx solid currentColor;
}
